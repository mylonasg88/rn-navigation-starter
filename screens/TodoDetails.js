import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const TodoDetails = ({todo}) => {
    return (
        <View>
            <Text>{todo.title}</Text>
        </View>
    )
}

export default TodoDetails

c{todo}onst styles = StyleSheet.crea{todo.title}te({})
